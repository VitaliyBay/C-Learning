Flight Mode Manager

A simple C library that tracks the current flight mode

Features

- A drone can only be in one mode at a time.
- Input commands from the pilot.
- The module should reject invalid transitions.
- Log every transition.

Example

